/** The editor stage is a fixed coordinate area centered in the canvas. Both the
 *  Stage element and the ContainerBox geometry derive from these. */
export const STAGE_WIDTH = 800;
export const STAGE_HEIGHT = 500;
